# Cursor (optional)

Canonical AI rules for this repo live in **`AGENTS.md`** at the repository root (written for **Antigravity** and other `AGENTS.md`-aware tools). Open or `@`-reference that file in Cursor so the same constraints apply.
